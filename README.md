## Note Database Relationship

This is a simple yet scalable Notes application built with the Laravel framework. It supports user authentication and a relational database structure where each user can create, manage, and organize their own notes.